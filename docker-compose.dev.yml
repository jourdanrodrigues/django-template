services:
  server:
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      - PRODUCTION=false
      - ALLOWED_HOSTS=*
    volumes:
      - .:/app

  database:
    volumes:
      - ./.dev-data/pgsql:/var/lib/postgresql/data
